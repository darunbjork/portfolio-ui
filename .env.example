# my-portfolio-os/frontend/.env.example

# Why: VITE_ prefixes are required for environment variables exposed to the client.
# This variable points to our backend API.
VITE_API_URL=http://localhost:3000/api/v1